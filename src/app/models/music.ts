export class Music {

  title: string;
  artist: string;
  album: string;

  constructor(title: string, artist: string, album: string) {
      this.title = title;
      this.artist = artist;
      this.album = album;
  }
}